
// This file now serves as a re-export from the refactored structure
// to prevent breaking existing imports throughout the codebase
export { LanguageProvider, useLanguage } from './language';
export type { Language, MeatType, LanguageContextType } from './language/types';
